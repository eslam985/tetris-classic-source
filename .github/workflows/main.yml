name: Build and Release

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.27.0' # تحديد الإصدار لضمان استقرار البناء ومنع أخطاء التوافق

      - name: Get dependencies
        run: flutter pub get

      - name: Build Web
        run: flutter build web --release --base-href "/tetris-classic-source/"
        # تأكد أن الاسم أعلاه "/tetris-classic-source/" هو نفس اسم المستودع عندك

      - name: Build Android APK
        run: flutter build apk --debug

      - name: Build Windows EXE
        run: flutter build windows --release

      - name: Upload Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: game-builds
          path: |
            build/app/outputs/flutter-apk/app-debug.apk
            build/windows/runner/Release/
            build/web/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web